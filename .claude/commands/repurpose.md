---
description: Repurpose an existing piece of content into other platforms/formats
---

Repurpose this content: **$ARGUMENTS**

(`$ARGUMENTS` may name an existing project, e.g. `content/carousels/<slug>`, or paste raw content.)

Follow `CLAUDE.md`:

1. Read `brand-kit/brand.json` for voice + links.
2. If it points to an existing project, read that project's `index.html` / `caption.md` to get the core message.
3. Produce a repurpose pack, adapted to each platform's norms and the brand voice:
   - **Reel / TikTok script** — hook, beats, on-screen text, CTA.
   - **YouTube** — title + description + a thumbnail text idea (offer to run `/thumbnail`).
   - **LinkedIn** — a professional rewrite of the same insight.
   - **X / Twitter thread** — 4–7 punchy posts.
4. Offer to turn any of these into a new asset (`/carousel`, `/story`, `/thumbnail`).
