# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
.vercel
*.tsbuildinfo

# env
.env*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# playwright
/test-results/
/playwright-report/

# ── user data: stays local, NEVER published ──
# each person generates their own brand + content; nothing of yours ships in the repo
/brand-kit/brand.json
/brand-kit/design-system.md
/public/content/*/*
/public/assets-library/*/*
# …but keep the empty folder skeleton so a fresh clone has somewhere to write
!**/.gitkeep
